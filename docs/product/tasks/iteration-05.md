# 迭代五 · Iteration 05


| 字段 | 值 |
|------|----|
| **状态** | **进行中（In Progress）** |
| **计划日期** | **2026-04-07** |
| **目标** | 以“把 Brainstorming-02 的方向收敛为可执行产品资产”为目标，优先推进壁纸工坊、智能配第一版策略、内容维护与数据更新机制设计、灵感入口与场景文案收敛，以及两项当前体验问题的规划与修复 |
| **范围来源** | 从 [Backlog.md](./Backlog.md) 捞出：`BL-BR-08`、`BL-BR-09`、`BL-BR-10`、`BL-BR-11`、`BL-UX-01`、`BL-BUG-01`；`BL-BR-03` 已于 2026-04-07 回退 Backlog |


---

## 迭代内 Issue


| Key | Type | Priority | Summary | Status | Acceptance（摘要） |
|-----|------|----------|---------|--------|-------------------|
| BL-BR-03 | Story | P2 | 精调：「智能配」主路径 + 可展开完整微调 | Backlog | 已完成部分数据/规则底座，但因当前搭配质量与 UI 交互未达标，2026-04-07 起暂停对外投放并屏蔽首页入口 |
| BL-BR-08 | Task | P3 | 运营侧内容管理方案调研与选型 | In Progress | 已从“Web 管理端”进一步收敛为“预览优先的配色策展工作台”方向；首版建议独立新开本地 Web 工程，先完成预览、筛选、对比、状态沉淀 |
| BL-BR-09 | Epic | P2 | 壁纸工坊（当前设备尺寸 / 拼合 / 渐变） | In Progress | 已确认进入 Iteration-05：首版聚焦“当前 palette -> 当前设备尺寸壁纸 -> 保存到相册”，不改底部保存 / 分享层级；基础样式免费，高级渐变与 IAP 暂不进入首版 |
| BL-BR-10 | Story | P2 | 配色数据更新机制设计（内置 / 远端覆盖 / CMS） | In Progress | 已明确前期以内置数据包发布为主；中期建议形成“资产仓库 source/bundle -> 工作台 -> App 导入脚本”的分层链路，再评估 GitHub 资产仓库接入 |
| BL-BR-11 | Task | P2 | 灵感入口与场景文案收敛 | Done | 已完成产品方案、PRD 回流、UI 结构草案与首页首版实现；当前按完成态关闭 |
| BL-UX-01 | Task | P1 | 精调能力升级与交互方案设计 | Done | 已完成交互方案收敛、代码原型实现与本轮验收回填；当前按完成态关闭 |
| BL-BUG-01 | Task | P1 | TopBar 日期与系统日期不同步修复 | Done | 已改为基于系统当前日历日动态格式化，并补前后台与跨午夜刷新链路 |


---

## 范围边界

- 本迭代重点是**策略落地、数据方案和文案收敛**，不是立即扩充大量新套组。
- `BL-BR-07` 本轮明确**不进入范围**；当前仍缺少稳定的扩展依据与生产节奏，不宜先做内容扩容。
- `BL-BR-09` 首版仅承诺**静态壁纸工坊**：当前设备尺寸、基础拼合 / 渐变样式、保存到相册；不在本轮承诺系统级一键设壁纸与新增付费打断。
- `BL-BR-03` 的已实现部分暂保留为底层探索资产，但当前不再作为本轮对外交付；待搭配质量与 UI 交互同时达标后，再从 Backlog 重新捞取。
- `BL-UX-01` 本轮先做**交互方案与能力边界定义**，不直接承诺在本轮完成完整精调重做。
- `BL-BUG-01` 属于当前体验正确性问题，应优先于样式层微调处理。
- 本迭代允许新增设计/架构/数据文件方案文档，但不要求一次性完成完整后端或 CMS 接入。
- 如果需要引入远端数据覆盖，本轮先定方案边界，不直接把架构做重。


## 建议执行顺序（非强制）

1. **BL-BR-10**：先明确数据更新机制，不然后续资产结构与内容维护方案会反复改口。
2. **BL-BR-09**：先把壁纸工坊的入口、主流程、样式边界与导出链路定清，再进入 UI 骨架与渲染实现。
3. **BL-BUG-01**：先修正 TopBar 日期错误，避免当前 UI 对系统状态表达失真。
4. **BL-UX-01**：在进入下一轮实际改造前，先把精调升级方案收敛清楚。
5. **BL-BR-08**：在更新机制边界清楚后，再定低成本维护流程与工具选型。
6. **BL-BR-11**：收敛灵感入口与“今天的故事”体系，避免内容资产和 UI 文案脱节。
7. **BL-BR-03**：在场景语义、候选池和数据结构稳定后，再推进智能配规则与实现切片。


## 当前进展（2026-04-07 晚）

- `BL-BR-03A / 03B` 已完成：颜色资产输入字段已补齐，`collections` 与 `catalog repository` 已进入当前数据流。
- `BL-BR-03C / 03D` 已完成：第一版硬规则、软排序和兜底回退已在代码中落地。
- `BL-BR-03F / 03G / 03I` 曾完成首版入口、结果解释层与抽屉动效探索，但已于 2026-04-07 从用户侧回退，首页“扩展搭配”入口现已屏蔽。
- 用户侧命名曾从“智能配”收敛为“扩展搭配”，以避免误导为开放式随机生成；当前功能先不对外暴露。
- 解释模板与场景口径已从代码中抽到 `entry/src/main/resources/rawfile/smartmatch/strategy.v1.json`，形成第一版可维护配置。
- `BL-BR-03E` 当前为部分完成：现有应用场景 `workday / weekend / outdoor / dinner` 已有模板化策略；后续若恢复功能，只继续围绕现有 app 场景补齐策略配置与解释口径。
- `BL-BR-03H` 尚未开始：扩展搭配结果进入完整精调链路后的可回退与手动逐项调整，保留到后续重新评估时再做。
- `BL-BR-08` 已启动：已从单纯 JSON 维护，收敛到“独立 Web 管理端 + GitHub 数据包 + App 导入脚本”的前期运营载体方向。
- `BL-BR-10` 已形成阶段性方案文档，并收敛出“前期以内置数据包为正式发布源”的实现路径；`BL-BR-11` 已完成产品收敛，并已落一版首页内联灵感入口原型用于验证。
- `BL-BUG-01` 已完成：TopBar 已从静态本地化日期文案改为基于系统日历日动态格式化，并补前后台恢复与跨午夜自动刷新。
- `BL-UX-01` 本轮新增：需要先把精调能力边界、交互形态与状态流转方案定稿，再决定实现拆分。

## 当前进展（2026-04-08）

- `BL-UX-01` 已完成首版代码原型：精调从原先三个裸 `TextInput` 升级为 `主色 / 辅色 / 点缀` 三角色切换、推荐色候选、手动 HEX 兜底、撤销上一步、恢复套组色与应用确认。
- 精调弹层已从页面内手搓全屏遮罩，切换为 ArkUI 系统 `bindSheet`；当前由系统处理蒙层、开合动画、外部点击关闭与键盘避让，已移除“mask 跟着整块面板生硬平移”的旧表现。
- 为支撑新精调交互，ViewModel 已补 `getActivePaletteBaseColors()` 与 `getFineTuneCandidateHexes()`，当前候选色来源于：当前显示色、当前套组基色、当前场景推荐 palette 的同角色颜色。
- 本轮同步补齐了新的精调文案键：`fineTuneSuggested / fineTuneManual / fineTuneUndo`，并在中英文内置文案中落地。
- 精调面板随后又完成了一轮视觉与交互收敛：`HEX` 输入已降级为默认折叠的“高级输入”，次级按钮改为等宽、弱强调样式；主界面 palette 卡、精调角色卡和推荐色圆点的选中态已从“黑色粗边框”调整为更轻的描边、微弱抬升与 ring/glow 反馈。
- 本轮继续补齐了精调应用后的即时反馈：应用成功后会根据结果显示“已应用微调 / 已恢复套组色” toast；同时首页 palette 卡已回退为原始上下分布与统一色条高度，不再保留常驻“已微调”标识，改为仅通过更克制的边框、底色、阴影与色条对比表达当前选中态。
- 为避免已缓存或远端下发的旧 locale bundle 缺字段导致首页启动闪退，`LocaleData.ets` 已改为对 UI 文案做逐字段安全回退；缺字段或空字符串时自动退回内置默认文案。
- `TodayStateStore.ets` 已补 `pref.flush()` 的异常兜底，避免 ArkTS 对可能抛错调用给出告警时阻断编译。
- 本轮处理过的编译 / 运行问题已清零：包括 ArkTS 对对象 spread、indexed access type、`bindSheet.radius` 裸数字类型，以及旧 locale bundle 缺字段导致的运行时 `TypeError`。
- 当前代码侧状态：文件级检查已通过；原型实现、交互收敛与本轮验收回填已完成，`BL-UX-01` 按完成态关闭。

## 当前进展（2026-04-09）

- `BL-UX-01` 本轮继续收敛状态表达：首页不再尝试把“已微调”常驻挂在 palette 卡片上，避免打乱卡片原始布局；当前改为把状态提示收敛到“精调”按钮本身，默认居中，进入微调态后再显示轻量状态提示。
- 精调面板的动作可用性已补齐：`恢复套组色` 仅在当前预览偏离套组基线时可点，`应用` 仅在当前预览相对已生效结果存在实际变化时可点，避免“未改也能点”的误导。
- 随机选链路已补结果可见化反馈：当命中的 palette 不在当前横向列表视窗内时，列表会自动滚动到对应卡片，并对命中卡片做一次短暂强调，减少用户在随机后“不知道刚刚切到了哪一套”的停顿。
- 当前随机选的列表联动实现基于固定卡片宽度与间距计算偏移；对每个场景未来扩展到几十至上百个 palette 仍可支撑，当前不需要为此额外引入更重的虚拟列表方案。
- `BL-BR-11` 已完成收口：灵感入口已完成任务重命名、PRD 回流、产品方案与 UI 草案沉淀，并在首页完成首版内联实现、日级稳定轮换候选池、文案降预期与展开动画收敛。
- 经本轮确认，`BL-UX-01` 已完成：当前精调能力按既定边界交付，不再继续保留为“方案 + 原型”状态。
- `BL-BR-08` 与 `BL-BR-10` 本轮形成新的阶段性结论：对即将进入“多场景 + 每场景几十到上百个配色盘”的内容运营阶段，最值得优先建设的不是传统 Web 后台，而是**预览优先的配色策展工作台**。
- 当前建议已从“独立 Web 管理端 + GitHub 数据包 + App 导入脚本”进一步收敛为：**先新开独立工作台工程，首版按本地 Web 工具实现，重点解决批量预览、筛选、对比、状态沉淀，再补 bundle 构建与远端资产仓库衔接。**
- 工作台首版推荐边界已明确：优先做 `总览 / 批量预览 / 对比 / 入选与淘汰管理 / 详情 / 发布前检查` 六类页面；优先沉淀 `reviewStatus / curationScore / rejectionReason / reviewNotes / isSelected` 等运营字段。
- 技术方案已形成首版建议：建议新开 `daypalette-palette-workbench` 独立工程，采用 `React + Vite + TypeScript + Node.js scripts + pnpm`；远端资产建议单独抽为 `daypalette-color-assets` 仓库，移动端继续只消费 `bundle/current/`。
- 本轮相关方案文档已完成收口；`strategy` 侧统一入口见 [`../strategy/README.md`](../strategy/README.md)，实现细化见 [`../../architecture/palette-curation-workbench-technical-implementation.md`](../../architecture/palette-curation-workbench-technical-implementation.md)。

## 当前进展（2026-04-17）

> 归档提示：本节记录的 `daypalette-palette-workbench` / `daypalette-color-assets` 推进状态已于 2026-04-29 归档，对应仓库与脚本不再属于当前规划。

- `BL-BR-08 / BL-BR-10` 已从纯规划进入首版代码实现：已在 `/Users/wuxinbo/Documents/Personal/daypalette-palette-workbench` 新建独立本地 Web 工作台工程，采用 `React + Vite + TypeScript + pnpm`。
- 工作台首版已完成单页骨架：覆盖 `总览 / 批量预览 / 对比池 / 详情侧栏 / 发布前检查` 五块高频工作区，当前重点仍是预览、筛选、对比与状态沉淀，而不是复杂表单后台。
- 当前样例源已直接接入 App 现有 `rawfile/palette-data` 三份真实 JSON：`base-colors.v1.json`、`palettes.v1.json`、`collections.v1.json`；工作台额外沉淀 `curation-state.v1.json` 作为运营层状态。
- 已补首版运营字段落地与文件化状态保存：当前界面可直接维护 `reviewStatus / curationScore / isSelected / rejectionReason / reviewNotes / readyForBundle / previewVariant`，除浏览器本地缓存外，也支持把当前策展状态导入 / 导出为 `curation-state.v1.json` 文件。
- 已补第二阶段仓库骨架：`/Users/wuxinbo/Documents/Personal/daypalette-color-assets` 已创建 `source/current/` 与 `bundle/current/` 两层目录，用于承接独立资产仓库形态。
- 已补四条首版脚本链路：`pnpm sync-app-source` 用于把 App 仓库 palette-data 同步到资产仓库 `source/current/` 并镜像到工作台，`pnpm validate` 用于 schema 与发布前检查，`pnpm build-bundle` 用于从 source + curation 状态生成工作台与资产仓库的 `bundle/current/`，`pnpm import-bundle-to-app` 用于把消费态 bundle 导回 App `rawfile/palette-data/`。
- 当前 `build-bundle` 与 `import-bundle-to-app` 已跑通首版输出：按照 `readyForBundle = true`、`isSelected = true`、`reviewStatus != rejected` 的边界生成消费态 bundle，并自动裁剪 palette / collection / base-color 依赖；当前 App 侧 `rawfile/palette-data/manifest.json` 也已随导入脚本进入资源目录。
- 本轮实现态验证已通过：当前工程 `pnpm lint`、`pnpm sync-app-source`、`pnpm validate`、`pnpm build-bundle`、`pnpm import-bundle-to-app` 均已跑通；当前仅保留 1 条预期内 warning，用于提示 `w5` 仍带人工校验标记 `spring-only`。
- 当前仍未进入的范围保持不变：尚未接远端 GitHub 资产仓库、多人协作、复杂编辑表单、在线 CMS、桌面端壳；这些继续保留到下一阶段再评估。

## 当前进展（2026-05-07）

- `BL-BR-09` 已从 Brainstorming 的远期项正式捞入 Iteration-05，并完成首轮文档定稿：PRD 已补功能边界，Backlog 已改排期，策略文档已新增 [../strategy/wallpaper-studio.md](../strategy/wallpaper-studio.md)。
- 壁纸能力当前统一定位为“当前 palette 的周边输出模式”，不替代现有 1:1 分享图，也不改底部保存 / 分享主操作层级。
- 入口方向已收敛：优先放在首页 palette 动作区之后，以轻量入口卡进入独立的全屏壁纸工坊。
- 首版样式范围已收敛为四类基础静态样式：三色拼合纵向分区、纸片拼贴错位层叠、三色线性渐变、柔雾空气渐变；每类至少三种策展化变体。
- 导出边界已收敛：使用独立于当前 `templateId` 的渲染分支，输出当前设备尺寸，首版只承诺保存到相册，不承诺系统级一键设为壁纸。
- 情绪价值方向已补齐：首版将同时补样式命名、极短情绪文案、锁屏想象预览与更有仪式感的保存反馈。
- `BL-BR-09B / 09C` 已完成首轮代码落地：首页已补壁纸入口卡、全屏壁纸工坊、独立壁纸快照与当前设备尺寸渲染链路；保存流程已通过独立 `WallpaperActions / WallpaperImageExporter` 分支接入相册保存。
- 文案与兜底当前已补齐：中英文 UI 文案、`LocaleData` fallback 与 `WallpaperSnapshot` 运行时边界已同步回流到代码与架构文档。
- 工坊 UI 已进入收口阶段：当前已完成多轮布局与交互调整，包括入口上移、顶部毛玻璃栏、顶部保存按钮、去底部保存栏、主预览去阴影，以及优先保证“顶部栏以下固定展示”的页面结构。
- 当前剩余重点已从“补骨架”切换为“IDE / 真机验收与细节修整”：重点看跨设备高度是否稳定占满、浮层是否存在触摸透传、样式切换与保存链路在真机上的一致性是否足够稳。

## 明日接手建议（跨设备继续）

1. 先在 IDE / 真机重点验 `BL-BR-09` 当前实现：检查壁纸工坊是否真正固定在顶部栏以下、背后首页是否仍会跟随滚动、顶部保存按钮在不同语言与设备宽度下是否稳定。
2. 继续收口当前交互细节：逐项验证样式切换、`换一版`、`纯净预览 / 锁屏想象` 与主预览同步关系，必要时再补更稳的状态重建或手势拦截。
3. 在当前保存链路稳定后，再评估下一步是补更多样式变体、安全区微调，还是增加“分享壁纸 / 设为壁纸”这类后续能力；不要在真机验收前继续扩功能面。


## 每项任务的方案设计

### BL-BR-10 · 配色数据更新机制设计（内置 / 远端覆盖 / CMS）

**目标**

- 明确 DayPalette 的配色资产未来如何更新，避免“每次调 palette 都必须重新发版”成为长期阻塞。

**方案设计**

- 采用分阶段评估：
  - `阶段 0`：完全内置，继续使用随应用版本发布的数据。
  - `阶段 1`：内置基础库 + 远端 JSON 覆盖，用于低成本更新配色盘与专题。
  - `阶段 2`：仅当多人协作与高频内容更新成立时，再考虑 CMS。
- 当前建议补充为：
  - `阶段 1 验证版`：可先用 `GitHub Pages` 或免费静态托管验证远端覆盖链路。
  - `阶段 1 正式低成本版`：优先使用**对象存储直出静态 JSON**，而不是先做服务端系统。
- 方案文档需明确：缓存、签名或版本字段、失败回退、离线降级、兼容老版本客户端的边界。
- 与当前 `LocaleData.ets` 的真实实现对照，产出“从硬编码迁移到文件化资源”的过渡路线。
- 低成本技术方案与部署对比详见 [`daypalette-docs/operations/remote-json-rollout-plan.md`](../../../../daypalette-docs/operations/remote-json-rollout-plan.md)；客户端接入补充见 [../../architecture/low-cost-remote-json-stage1-plan.md](../../architecture/low-cost-remote-json-stage1-plan.md)。
- 结合 2026-04-09 的新结论，前期正式发布源仍以 App 内置 bundle 为准；但中期数据链路应开始按 `source/ -> workbench -> bundle/current/ -> App 导入脚本` 设计，避免未来资产规模上来后再次推翻。
- 远端 GitHub 资产仓库当前不作为首版前提，而作为第二阶段接入项；首版先跑通本地 source 数据、校验和 bundle 构建闭环。

**交付物**

- 一份分阶段选型结论。
- 一份真实数据文件路径与加载方式草案。
- 一份“管理端导出 -> GitHub 数据包 -> App 导入脚本 -> 随版本发版”的发布链路说明。
- 一份低成本可落地方案对比表（`GitHub Pages / 对象存储 / 静态托管 / 云函数`）。
- 一份远端覆盖的风险清单与降级策略。


### BL-BR-08 · 运营侧内容管理方案调研与选型

**目标**

- 在不引入重 CMS 的前提下，形成一套主理人可长期维护的配色资产工作流。

**方案设计**

- 先把“编辑态 / 发布态 / 审核态”三段流程写清，再决定是否需要工具。
- 编辑态优先看结构化表格；发布态优先看仓库内 JSON；工具选型只作为后续增强项。
- 审核口径需覆盖：命名一致性、场景适配、色彩风险、资产重复度。
- 当前进一步收敛为：以前期**预览优先的配色策展工作台**作为主要工作载体，而不是传统表单后台。
- 工具形态当前建议为：先新开独立本地 Web 工程，优先实现批量预览、筛选、对比、状态标记与发布前检查；编辑与发布能力作为第二优先级补齐。
- GitHub 资产仓库继续保留为中期目标，用于承载 `source / bundle / schema / manifest`；但首版工具不强依赖远端仓库，先跑通本地工作区闭环。

**交付物**

- 一份低成本维护流程说明。
- 一份管理端信息架构与页面草案。
- 一份审核清单模板。

**当前进展（2026-04-07）**

- 已确定“代码内规则引擎 + `rawfile` 策略 JSON”作为当前最小可维护方案。
- 运营当前可直接维护的核心文件为 `entry/src/main/resources/rawfile/smartmatch/strategy.v1.json`。
- `strategy` 目录已完成一轮收口：当前统一入口见 [../strategy/README.md](../strategy/README.md)；现行主文档优先看 [../strategy/color-asset-unified-master-plan.md](../strategy/color-asset-unified-master-plan.md)、[../strategy/palette-curation-workbench.md](../strategy/palette-curation-workbench.md) 与 [../strategy/conservative-smart-matching-strategy.md](../strategy/conservative-smart-matching-strategy.md)。
- 早期资产操作方案已归档到 [../strategy/archive/color-asset-operations-plan.md](../strategy/archive/color-asset-operations-plan.md)，仅保留作历史参考。
- 2026-04-09 新增阶段性收敛：
  - 已补技术实现方案：[../../architecture/palette-curation-workbench-technical-implementation.md](../../architecture/palette-curation-workbench-technical-implementation.md)。
- 后续仍需补齐的落地物已从“传统管理端信息架构”转为：数据 schema 草案、工作台工程初始化方案，以及面向实现的迭代拆分。


  ### BL-BR-09 · 壁纸工坊（当前设备尺寸 / 拼合 / 渐变）

  **目标**

  - 基于用户当前选中的三色，生成当前设备尺寸的手机壁纸，让当前 palette 从“今天的配色”延展为“今天的桌面情绪产物”。

  **方案设计**

  - 壁纸能力当前定位为“当前 palette 的周边输出模式”，不替代现有 1:1 色卡出图。
  - 首页入口优先放在 palette 动作区之后，不改底部保存 / 分享主操作层级。
  - 点击入口后进入独立的全屏壁纸工坊，优先展示当前设备比例预览，而不是复用正方形画布。
  - 首版基础样式收敛为四类：三色拼合纵向分区、纸片拼贴错位层叠、三色线性渐变、柔雾空气渐变。
  - 每类样式至少提供 3 个策展化变体，用“换一版”承接轻量探索，不暴露大量自由参数。
  - 工坊内可提供“纯净预览 / 锁屏想象”两种查看方式，但锁屏想象层不写入导出文件。
  - 渲染链路建议独立于当前 `templateId` 体系，仅复用当前 palette 快照、颜色工具与相册保存能力。
  - 首版基础样式全部免费；高级渐变、动态壁纸、风格包与系统级一键设为壁纸留待后续单独评估。

  **交付物**

  - 一份完整的产品 / UX 方案文档：[../strategy/wallpaper-studio.md](../strategy/wallpaper-studio.md)。
  - 一份首页入口与工坊页的信息架构草案。
  - 一份壁纸样式枚举、变体边界与情绪文案包装清单。
  - 一份与现有导出系统的拆分建议，明确哪些能力可复用、哪些必须新建。

  **验收口径（本轮）**

  - 明确入口不占用底部保存 / 分享主操作位。
  - 明确首版只做基础静态壁纸，不承诺系统级一键设为壁纸。
  - 明确壁纸输出尺寸以当前设备尺寸为准，而不是继续复用 1080x1080。
  - 明确基础样式、变体数量与情绪化包装的首版边界。
  - 产出可直接供下一步 ArkUI 实现拆分使用的状态与渲染边界说明。

  **当前进展（2026-05-07）**

  - 已完成首轮文档定稿：PRD、Backlog、Iteration-05 与 strategy 索引已同步回流。
  - 已补首版方案文档：[../strategy/wallpaper-studio.md](../strategy/wallpaper-studio.md)。
  - 已完成 `BL-BR-09B / 09C` 首轮实现：首页入口卡、全屏工坊、独立壁纸快照、设备比例预览与保存链路当前均已入代码。
  - 工坊页当前已进入 UI 收口：正在围绕固定布局、顶部保存按钮、毛玻璃栏、样式切换同步与跨设备高度适配做连续微调。
  - 下一步优先级已调整为：先做 IDE / 真机验收与细节修整，再决定是否继续扩样式、分享链路或系统级设壁纸能力。


### BL-BR-11 · 灵感入口与场景文案收敛

**目标**

- 为“今天没有固定安排，只想先看看色彩灵感”的用户补一个轻量承接入口，同时保持当前场景主路径不被打散。
- 让用户在不增加更重前置决策层的前提下，更容易理解不同场景的情绪与使用边界。

**方案设计**

- 继续保留单层场景入口，不拆成“有安排 / 随便走走”双入口。
- 不把“今天没安排”直接固化成与 `workday / weekend / outdoor / dinner` 并列的正式场景，而优先评估轻量灵感入口。
- 灵感入口优先承接“先看看、先找感觉”的状态，再把用户导回现有 palette 主链路。
- 重点优化入口文案、Hero 文案、场景标题与专题标题之间的语义一致性。
- 为“随便看看 / 给我一点灵感 / 今天没安排”这类状态给出更自然的产品表达。

**交付物**

- 一份灵感入口产品草稿：包含放置位置、交互流与入口边界。
- 一份入口文案与场景命名候选清单。
- 一份 Hero / 场景 / 专题文案对齐表。

**验收口径（本轮）**

- 明确灵感入口不是新的正式场景，不与现有 `occasionId` 体系混层。
- 明确入口首选放置位置、展开方式与关闭后的回落行为。
- 明确首版内容来源与展示数量边界，避免为入口单独引入新数据模型。
- 明确与 `随机选`、正式场景、专题策展三者的区别。
- 产出可直接供下一轮设计或开发继续推进的 UI 结构草案与最小实现清单。

**当前进展（2026-04-09）**

- 已完成文档侧收口：灵感入口方案、UI 结构与最小实现清单当前统一见 [../strategy/inspiration-entry.md](../strategy/inspiration-entry.md)，并已回流 PRD，明确“今天没安排”由轻量灵感入口承接，不新增正式场景。
- 已完成首页首版实现：入口位于 Hero 与场景选择之间，采用内联展开方式，选中后回落到现有 palette 主链路。
- 已完成首版展示池策略收敛：不使用固定白名单，改为基于现有可用 palette 池的“当日稳定轮换”。
- 已完成入口文案与动效收敛：采用更克制的“今日灵感 / Today's Picks”口径，并补齐展开与收起过渡。
- 结合产品、文档与代码状态，`BL-BR-11` 当前按完成态关闭；后续若继续优化，仅另开体验 polish 任务，不再挂在本项下。


### BL-BUG-01 · TopBar 日期与系统日期不同步修复

**目标**

- 保证 TopBar 显示日期始终与系统当前日期一致，不因跨天、前后台切换、系统时区变化或语言切换而滞后。

**方案设计**

- 先确认当前 TopBar 日期字符串的真实来源，是直接读系统时间、读缓存、还是复用 ViewModel 中的今日快照状态。
- 检查以下触发链路是否都会刷新日期：应用冷启动、应用回前台、跨午夜、系统时区变化、系统语言变化。
- 若当前日期依赖“今日配色快照”而不是独立的日历日边界，应把“展示日期”和“配色快照日期”拆开，避免 UI 错把业务缓存当系统时间。
- 修复后要确认 TopBar、Hero 副标题、昨日功能判定三者对“今天/昨天”的定义保持一致。

**交付物**

- 一份问题定位结论。
- 一份修复方案或实际修复结果。
- 一份覆盖冷启动、回前台、跨天、时区切换的验证清单。

**当前进展（2026-04-07）**

- 已定位根因：TopBar 直接使用 `LocaleData` 中的静态 `ui.date` 示例文案，未绑定系统当前日期。
- 已改为由 ViewModel 基于系统当前日历日动态格式化日期字符串，不再依赖静态文案。
- 已补两条刷新链路：应用回前台时刷新日期；应用保持前台跨午夜时，自动在下一次本地午夜后刷新。
- 待用户在 IDE 中实际运行确认时区切换场景；当前已完成代码侧修复与文件级检查。


### BL-UX-01 · 精调能力升级与交互方案设计

**目标**

- 为精调功能制定一版可执行的升级方案，让用户既能更自由地改色，又不会因为交互过重而失去 DayPalette 的轻量感。

**方案设计**

- 先明确能力边界：第一版升级建议允许用户分别修改 `primary / secondary / accent` 三个角色颜色，但不开放无限自由编辑的复杂面板作为默认主路径。
- 用户可改的内容建议分三层：
  - `层 1`：直接替换当前三格中的任一角色颜色。
  - `层 2`：基于当前角色给出安全候选色，优先从已审核 `BaseColor / Palette` 资产中选，不鼓励直接裸输任意 HEX 作为主路径。
  - `层 3`：保留进阶入口处理手动 HEX / 最近使用 / 恢复默认，但放在二级层，不抢主流程。
- 交互形式建议优先评估“底部 Sheet + 角色分段切换 + 候选色网格”的结构，而不是三个输入框直改：
  - 进入精调后先看到当前三色预览与 `主色 / 辅色 / 点缀色` 分段选择。
  - 选中某一角色后，下方展示该角色的安全候选色网格，以及少量按场景过滤后的推荐色。
  - 顶部保留“恢复当前套组”“撤销上一步”“应用”三种清晰动作，减少误操作。
- 状态流转建议：
  - 用户进入 Sheet 时以当前 palette 为基线。
  - 在 Sheet 内的改动先作为临时预览，可实时映射 Hero。
  - 用户关闭时若有未提交改动，明确区分“应用修改 / 放弃修改”。
  - 若后续重新接回扩展搭配，精调也要能识别“当前基线来自推荐结果还是原始 palette”。
- 需要单独说明不做什么：
  - 不把精调做成完整专业调色板工具。
  - 不在首版默认暴露过多高级参数，如饱和度滑杆、明度曲线、色轮自由选点。

**交付物**

- 一份精调升级交互草案。
- 一份“用户可修改颜色范围”定义清单。
- 一份状态流转说明：进入、预览、撤销、恢复、应用、关闭。
- 一份后续实现拆分建议（低风险版 / 完整版）。

**当前进展（2026-04-08）**

- 已完成首版交互原型：三角色切换 + 推荐色候选 + 手动 HEX + 撤销 + 恢复套组色 + 应用。
- 当前弹层实现已从自绘遮罩改为 ArkUI 系统 `bindSheet`，用于改善蒙层和开合动画的一致性。
- 已完成第二轮界面收敛：默认隐藏高级输入，补齐次级操作按钮版式，并统一主界面与精调面板的选中态语言。
- 已补应用结果反馈：精调应用成功后按“仍为微调态 / 已回到套组基线”分别给出 toast；首页 palette 卡的常驻微调标识已取消，避免干扰原始卡片布局与色条区域。
- 当前首页 palette 卡已恢复原版节奏：色条与标题重新按上下两端分布，选中态不再通过色条变粗或额外角标表达，而是保留轻量的卡片边框、底色、阴影与色条对比差异。
- 已补按钮与动作状态约束：主页“精调”按钮默认保持居中，仅在已微调状态下显示轻量提示；Sheet 内 `恢复套组色` / `应用` 已改为按当前是否存在实际变更决定可用性。
- 已补随机选的结果可见化：随机命中不可见 palette 时，横向列表会自动滚动到命中项，并对目标卡片做一次短暂强调反馈。
- 运行时与编译兼容问题已完成修复，当前待补的是交互体验验证，而不是基础可用性修复。


### BL-BR-03 · 精调：「智能配」主路径 + 可展开完整微调

**目标**

- 把“智能配”从头脑风暴概念落成第一版可执行方案，并为后续编码实现切出低风险路径。

**方案设计**

- 第一阶段优先做规则型方案，而不是开放式生成。
- 数据前提依赖：基础单色候选池、场景语义、资产标签和安全等级。
- 若实现开始后发现“规则引擎 + 新交互 + 大量数据整理”已经超过单轮可控范围，则在本迭代完成规则与数据基础，把 UI / 交互落地顺延到后续迭代。

**实现切片**

| 子项 | 分类 | 优先级 | 目标 | 完成定义 |
|-----|------|--------|------|---------|
| BL-BR-03A | Data | P0 | 智能配输入字段补齐 | 为颜色资产补齐主色候选池分层、场景标签、风险等级、解释文案所需字段 |
| BL-BR-03B | Data | P1 | 数据读取过渡层准备 | 明确 `LocaleData.ets`、未来 `rawfile/palette-data/*.json` 与 `catalog repository` 的兼容加载方案 |
| BL-BR-03C | Rules | P0 | 第一版硬规则引擎 | 可用配置或常量实现禁配、明度跨度、脏色保护、场景黑白名单与兜底回退 |
| BL-BR-03D | Rules | P1 | 第一版软规则与排序 | 在合法候选内完成 tone 权重、场景偏好、专题加分、去重与排序 |
| BL-BR-03E | Rules | P1 | 场景策略模板 | 完成现有 app 场景的策略配置、解释模板与口径收敛；不承接专题策展方向 |
| BL-BR-03F | UI | P0 | 智能配主入口接入 | 在精调主路径中增加“先选主色，再智能补全”的入口，不增加前置决策层 |
| BL-BR-03G | UI | P1 | 推荐结果解释层 | 结果卡片展示推荐原因、适用语境、失败提示和空状态 |
| BL-BR-03H | UI | P1 | 完整微调保留 | 智能配结果进入现有精调链路后仍可逐项手动调整，且可回退 |
| BL-BR-03I | UI | P2 | 优化扩展搭配抽屉动画 | 遮罩单独淡入淡出，内容区具备更柔和的上浮/收起反馈，不与整块抽屉硬滑动绑定 |

**推荐依赖顺序**

1. `BL-BR-03A`：先把输入字段补齐，否则规则无法稳定落地。
2. `BL-BR-03B`：明确过渡读取层，避免后续 UI 先绑死在硬编码数据上。
3. `BL-BR-03C`：先做硬规则，先保证“不出错”。
4. `BL-BR-03D`、`BL-BR-03E`：在合法候选上再做场景化排序与解释。
5. `BL-BR-03F`、`BL-BR-03G`、`BL-BR-03H`：最后接入 UI 主路径与完整微调。

**交付物**

- 一份第一版候选池与规则清单。
- 一份场景级策略与解释文案模板。
- 一份编码切片建议（数据、规则、UI 分阶段）。
- 一份 `BL-BR-03A ~ BL-BR-03H` 的实现顺序与迭代去向建议。

**当前进展（2026-04-07）**

- `BL-BR-03A` 已完成：`Palette` / `Collection` 的规则字段、风险等级、style tags 和颜色 profile 已接入。
- `BL-BR-03B` 已完成：`collections`、`catalog repository` 与 `rawfile` 方案已进入现有加载链路。
- `BL-BR-03C` 已完成：第一版硬规则已覆盖高饱和冲突、双深色沉闷、accent 越权、场景禁忌色与实验性资产拦截。
- `BL-BR-03D` 已完成：第一版软排序已覆盖安全等级、场景贴合、风格标签与层次稳定性。
- `BL-BR-03E` 部分完成：模板机制与第一批场景配置已落地；后续若恢复功能，只继续围绕现有 app 场景补齐策略和解释口径。
- `BL-BR-03F / 03G` 已完成：入口、结果卡和失败/空状态已接入；当前形态为底部抽屉。
- `BL-BR-03I` 已完成：抽屉动效已从整块平移，升级为分层遮罩与内容动画。
- `BL-BR-03H` 待继续：后续需要把“应用扩展搭配后继续精调与回退”的路径补完整。


## 本轮不做 / 回到 Backlog

- **BL-BR-03：精调「智能配 / 扩展搭配」主路径**：2026-04-07 起回退 Backlog。原因是当前搭配质量与 UI 交互未达到可交付标准；已保留底层规则与数据资产，但先屏蔽首页入口，后续再评估恢复。
- **BL-UX-01 的完整实现落地**：本轮先收敛方案与边界，不承诺在本轮把精调升级全部编码完成。
- **BL-BR-07：预置套组与场合扩容**：当前不进入 iteration-05，待扩展依据、生产方法和更新节奏更清晰后再启动。
- **大规模内容上新**：本轮以结构、规则与维护方案为主，不追求一次性扩到大量新 palette。
- **完整 CMS 接入或后端服务建设**：本轮先定方向，不做重实现。


## 完成时

- 将上表各 Issue 的 `Status` 更新为真实状态。  
- 若 `BL-BR-03` 最终拆到后续迭代，应在 [Backlog.md](./Backlog.md) 中明确新的排期去向。  
- 将真实数据文件方案同步回 [../../architecture/architecture.md](../../architecture/architecture.md) 与 [../../architecture/data-model.md](../../architecture/data-model.md)。  
- 将产品层收敛结论同步回 [../PRD.md](../PRD.md) 或相关策略文档。
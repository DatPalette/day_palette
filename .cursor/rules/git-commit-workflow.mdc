---
description: 提交代码流程：diff 审查、拆分 commit、中文 emoji 规范、安全与暂存纪律
alwaysApply: true
---

# Git 提交（用户要求「提交 / 提代码 / commit」时）

## 1. 先看变更

- 执行并阅读 `git status` 与 `git diff`（工作区 + `git diff --cached`），确认意图、范围、无意外文件（例如误改的本地配置、调试残留）。
- 若存在未跟踪文件，判断应提交还是应写入 `.gitignore`；**勿**把构建产物、IDE 私有状态、密钥或证书类文件加入版本库。

## 2. 拆分 commit

- 按**可单独回滚**的逻辑拆分：功能、修复、样式/UI、重构、文档、配置/工具链尽量分开。
- 与 `AGENTS.md` 一致：若同一次迭代既改 `docs/` 又改 `entry/`，优先 **先文档、后代码** 拆成两个 commit（例如 `📖 DOC:` 再 `📦 NEW:` / `👌 IMPROVE:`），除非用户要求单 commit squash 说明。
- 同一主题的多文件可放在一个 commit；**不要**把无关重构混进功能 commit。

## 3. 说明写法（中文 + emoji）

**主题行（第一行）**

- 使用**中文**；前缀为 **emoji + 英文类型 + 冒号 + 简短描述**（描述用中文）。
- **类型统一大写**，例如 `NEW:`、`IMPROVE:`、`FIX:`。
- 控制在约 **50 字以内**，陈述「做了什么」；避免结尾标点堆砌。
- 常用类型与 emoji 参考（按当前提交风格合并）：
  - `📦 NEW:` 全新功能、全新模块、从 0 到 1 的新增能力。原 `feat` 中明确属于“新增”的内容统一并入这里。
  - `👌 IMPROVE:` 改进、重构、样式优化、体验打磨、性能优化、文案微调、配置整理等非新增类改动。原 `refactor`、`style`、`perf`、`i18n`、部分 `chore` 统一并入这里。
  - `🐛 FIX:` 缺陷修复、回归修复、边界条件修正。若本次提交核心是修 bug，即使用该类型。
  - `📖 DOC:` 文档 / PRD / 设计说明 / 注释规范更新。原 `docs` 统一并入这里。
  - `🚀 RELEASE:` 发版、版本号、构建产物清单、发布流程、发布配置调整。原偏发布向的 `chore` 统一并入这里。
  - `🤖 TEST:` 测试相关提交，包括新增测试、修测试、测试基建。
  - `‼️ BREAKING:` 破坏性变更、需要协作者显式注意的接口或行为变化。
- 若原本会写成 `revert`，优先按实际目的归类到 `FIX:` 或 `BREAKING:`；只有在用户明确强调“回滚某次提交”时，正文中注明回滚对象。
- **破坏性变更**优先直接使用 `‼️ BREAKING:`，正文首行再补充影响范围。

**正文（可选）**

- 主题行空一行后写要点：动机、影响面、与 PRD/设计文档的对应关系（必要时）。
- 多个独立点用短列表，避免长篇散文。

**推荐映射**

- 新增页面、新增设置项、新增交互能力：`NEW:`
- UI 微调、代码整理、导出样式优化、顶栏/按钮体验调整：`IMPROVE:`
- 修复编译错误、运行时错误、逻辑 bug：`FIX:`
- 只改文档：`DOC:`
- 测试与验证脚本：`TEST:`
- 发版流程、版本号、发布构建：`RELEASE:`
- 行为不兼容、需要迁移说明：`BREAKING:`

**主题行示例**

- `📦 NEW: 新增微缩预览样式切换设置`
- `👌 IMPROVE: 优化今日页三个入口按钮布局`
- `👌 IMPROVE: 调整精选配色盘卡片内部留白`
- `🐛 FIX: 修复昨日快照跨多天误判问题`
- `📖 DOC: 更新迭代二遗留问题处理结论`
- `🚀 RELEASE: 更新 0.4.0 发布说明与版本号`
- `🤖 TEST: 补充昨日快照逻辑验证用例`
- `‼️ BREAKING: 调整导出模板字段结构`

## 4. 暂存与内容纪律

- 优先**按文件或按 hunk** 有选择地 `git add`，避免 `git add .` 把调试代码、注释掉的死代码、临时代码一并提交。
- 提交前快速扫一眼 diff：**移除** `console.log`、临时代码路径、个人路径；**不要**提交含密钥、token、密码、私有证书的内容。
- 鸿蒙工程注意：本地签名、路径类配置若已在 `.gitignore`，不要强行 `add -f`。

## 5. 何时执行 git 命令

- **仅在用户明确要求提交**（或明确说可以 commit）后执行 `git add` / `git commit`。
- **默认不** `git push`，除非用户明说推送。
- **不**随意 `git commit --amend` 改写已推送历史，除非用户明确要求且了解影响。

## 6. 与日常开发的衔接

- 若用户只要求改代码而未说提交：改完可提示「需要的话可以说提交」，**不要主动 commit**。
- 若工作区混杂多任务改动，先与用户确认本次提交范围，再拆分 commit。
